==> ./Dockerfile <==
FROM node:20-alpine as build

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]

==> ./index.html <==
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Crime OS AI — Agentic Intelligence Platform for Police Investigations</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
  </head>
  <body style="margin: 0; background-color: #0A0E1A; color: #F3F4F6;">
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>

==> ./package.json <==
{
  "name": "crime-os-ai-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@emotion/react": "^11.11.4",
    "@emotion/styled": "^11.11.5",
    "@mui/icons-material": "^5.15.20",
    "@mui/material": "^5.15.20",
    "axios": "^1.7.2",
    "framer-motion": "^11.2.10",
    "lucide-react": "^0.395.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.23.1",
    "recharts": "^2.12.7",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.3.1"
  }
}

==> ./src/App.jsx <==
import React, { useState } from 'react';
import { Routes, Route, useLocation } from 'react-router-dom';
import { Box } from '@mui/material';
import Navbar from './components/Navbar';
import Sidebar from './components/Sidebar';

import DashboardPage from './pages/DashboardPage';
import ComplaintsPage from './pages/ComplaintsPage';
import InvestigationPage from './pages/InvestigationPage';
import RequestsPage from './pages/RequestsPage';
import AnalyticsPage from './pages/AnalyticsPage';
import AdminPage from './pages/AdminPage';
import LoginPage from './pages/LoginPage';

export default function App() {
  const location = useLocation();
  const [user, setUser] = useState({
    username: 'io_patel',
    role: 'IO',
    full_name: 'PSI Inspector V. K. Patel',
    police_station: 'Surat Cyber Crime Station'
  });

  const isLoginPage = location.pathname === '/login';

  if (isLoginPage) {
    return <LoginPage onLogin={(u) => setUser(u)} />;
  }

  return (
    <Box sx={{ minHeight: '100vh', background: '#0A0E1A' }}>
      <Navbar user={user} />
      <Box sx={{ display: 'flex' }}>
        <Sidebar />
        <Box component="main" sx={{ flexGrow: 1, minHeight: 'calc(100vh - 64px)', overflowX: 'hidden' }}>
          <Routes>
            <Route path="/" element={<DashboardPage />} />
            <Route path="/complaints" element={<ComplaintsPage />} />
            <Route path="/investigation" element={<InvestigationPage />} />
            <Route path="/requests" element={<RequestsPage />} />
            <Route path="/analytics" element={<AnalyticsPage />} />
            <Route path="/admin" element={<AdminPage />} />
          </Routes>
        </Box>
      </Box>
    </Box>
  );
}

==> ./src/components/AgentFlowGraph.jsx <==
import React from 'react';
import { Box, Paper, Typography, Grid, Chip } from '@mui/material';
import { AutoAwesome, CheckCircle, Warning, Storage, Policy, VerifiedUser } from '@mui/icons-material';

export default function AgentFlowGraph({ activeStep = 4, status = 'APPROVED' }) {
  const steps = [
    { id: 1, title: 'Multimodal Ingestion', desc: 'PDF / Audio / Vision Parser', icon: <Storage /> },
    { id: 2, title: 'Cross-Case Memory', desc: 'Qdrant Serial Offender Link', icon: <Storage /> },
    { id: 3, title: 'Specialist Pods (Parallel)', desc: 'BNS, BSA, Cyber, Field Agents', icon: <AutoAwesome /> },
    { id: 4, title: 'Evaluator Anti-Laziness', desc: 'Grounding & Quality Filter', icon: <Policy /> },
    { id: 5, title: 'HITL Review', desc: 'Officer Approval', icon: <VerifiedUser /> },
    { id: 6, title: 'Synthesis & PDF', desc: 'Master FIR & Section 94 Notice', icon: <CheckCircle /> }
  ];

  return (
    <Paper className="glass-card" sx={{ p: 2.5, mb: 3 }}>
      <Box sx={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', mb: 2 }}>
        <Typography variant="h6" sx={{ fontWeight: 700, display: 'flex', alignItems: 'center', gap: 1 }}>
          <AutoAwesome sx={{ color: '#2563EB' }} /> LangGraph Agent Execution Pipeline
        </Typography>
        <Chip 
          label={status === 'APPROVED' ? 'EVALUATOR PASSED' : 'REJECTED (RETRYING)'} 
          color={status === 'APPROVED' ? 'success' : 'warning'}
          size="small"
          sx={{ fontWeight: 700 }}
        />
      </Box>

      <Grid container spacing={1.5} alignItems="center">
        {steps.map((step, idx) => {
          const isCompleted = step.id <= activeStep;
          const isCurrent = step.id === activeStep;
          return (
            <Grid item xs={12} sm={6} md={2} key={step.id}>
              <Box
                sx={{
                  p: 1.5,
                  borderRadius: 2,
                  textAlign: 'center',
                  background: isCurrent 
                    ? 'rgba(37, 99, 235, 0.25)' 
                    : isCompleted 
                    ? 'rgba(16, 185, 129, 0.15)' 
                    : 'rgba(255, 255, 255, 0.03)',
                  border: isCurrent 
                    ? '1.5px solid #2563EB' 
                    : isCompleted 
                    ? '1px solid rgba(16, 185, 129, 0.4)' 
                    : '1px solid rgba(255, 255, 255, 0.08)',
                  transition: 'all 0.3s ease'
                }}
              >
                <Box sx={{ color: isCurrent ? '#60A5FA' : isCompleted ? '#34D399' : '#9CA3AF', mb: 0.5 }}>
                  {step.icon}
                </Box>
                <Typography variant="caption" sx={{ display: 'block', fontWeight: 700, color: '#F9FAFB' }}>
                  {step.title}
                </Typography>
                <Typography variant="caption" sx={{ color: '#9CA3AF', fontSize: '0.7rem' }}>
                  {step.desc}
                </Typography>
              </Box>
            </Grid>
          );
        })}
      </Grid>
    </Paper>
  );
}

==> ./src/components/LinkAnalysisGraph.jsx <==
import React from 'react';
import { Box, Paper, Typography, Chip, Grid } from '@mui/material';
import { Hub, AccountTree, Warning } from '@mui/icons-material';

export default function LinkAnalysisGraph({ matches = [] }) {
  const defaultMatches = [
    { match_type: 'VPA_RECURRENCE', matched_value: 'scammer@paytm', previous_case_no: 'CR-2026-0812', police_station: 'Surat Cyber Cell', confidence: 0.94 },
    { match_type: 'PHONE_CDR_RECURRENCE', matched_value: '+91 98765 43210', previous_case_no: 'CR-2026-0441', police_station: 'Rajkot Rural Station', confidence: 0.88 }
  ];

  const displayMatches = matches.length > 0 ? matches : defaultMatches;

  return (
    <Paper className="glass-card" sx={{ p: 2.5, mb: 3 }}>
      <Box sx={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', mb: 2 }}>
        <Typography variant="h6" sx={{ fontWeight: 700, display: 'flex', alignItems: 'center', gap: 1 }}>
          <Hub sx={{ color: '#F59E0B' }} /> Qdrant Cross-Case Memory Link Analysis
        </Typography>
        <Chip label={`${displayMatches.length} Serial Matches Found`} color="warning" size="small" sx={{ fontWeight: 700 }} />
      </Box>

      <Grid container spacing={2}>
        {displayMatches.map((m, i) => (
          <Grid item xs={12} md={6} key={i}>
            <Box sx={{ p: 2, borderRadius: 2, background: 'rgba(245, 158, 11, 0.08)', border: '1px solid rgba(245, 158, 11, 0.3)' }}>
              <Box sx={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', mb: 1 }}>
                <Typography variant="subtitle2" sx={{ fontWeight: 700, color: '#FBBF24', display: 'flex', alignItems: 'center', gap: 1 }}>
                  <Warning fontSize="small" /> {m.match_type}
                </Typography>
                <Chip label={`${(m.confidence * 100).toFixed(0)}% Match`} size="small" sx={{ bgcolor: '#F59E0B', color: '#000', fontWeight: 800 }} />
              </Box>
              <Typography variant="body2" sx={{ fontFamily: 'monospace', color: '#F9FAFB', fontWeight: 700, mb: 0.5 }}>
                Value: {m.matched_value}
              </Typography>
              <Typography variant="caption" sx={{ color: '#9CA3AF', display: 'block' }}>
                Previous Linked FIR: <b>{m.previous_case_no}</b> ({m.police_station})
              </Typography>
            </Box>
          </Grid>
        ))}
      </Grid>
    </Paper>
  );
}

==> ./src/components/Navbar.jsx <==
import React from 'react';
import { AppBar, Toolbar, Typography, Box, Badge, IconButton, Avatar, Chip } from '@mui/material';
import { Notifications, Shield, LocalPolice } from '@mui/icons-material';

export default function Navbar({ user }) {
  return (
    <AppBar position="sticky" sx={{ background: '#111827', borderBottom: '1px solid rgba(255, 255, 255, 0.08)', boxShadow: 'none' }}>
      <Toolbar sx={{ justifyContent: 'space-between' }}>
        <Box sx={{ display: 'flex', alignItems: 'center', gap: 1.5 }}>
          <Shield sx={{ color: '#2563EB', fontSize: 32 }} />
          <Box>
            <Typography variant="h6" sx={{ fontWeight: 800, letterSpacing: 0.5, color: '#F9FAFB', lineHeight: 1.2 }}>
              Crime OS AI
            </Typography>
            <Typography variant="caption" sx={{ color: '#9CA3AF', fontSize: '0.75rem' }}>
              Intelligence-led Police Investigation Suite
            </Typography>
          </Box>
        </Box>

        <Box sx={{ display: 'flex', alignItems: 'center', gap: 2 }}>
          <Chip 
            icon={<LocalPolice sx={{ color: '#10B981 !important' }} />} 
            label="Surat Cyber Station" 
            variant="outlined" 
            sx={{ borderColor: 'rgba(16, 185, 129, 0.3)', color: '#10B981', fontWeight: 600 }}
          />

          <IconButton color="inherit">
            <Badge badgeContent={3} color="error">
              <Notifications />
            </Badge>
          </IconButton>

          <Box sx={{ display: 'flex', alignItems: 'center', gap: 1 }}>
            <Avatar sx={{ bgcolor: '#2563EB', width: 36, height: 36, fontWeight: 700, fontSize: '0.9rem' }}>
              IO
            </Avatar>
            <Box sx={{ display: { xs: 'none', sm: 'block' } }}>
              <Typography variant="body2" sx={{ fontWeight: 700, color: '#F9FAFB' }}>
                {user?.full_name || 'PSI V. K. Patel'}
              </Typography>
              <Typography variant="caption" sx={{ color: '#9CA3AF', display: 'block' }}>
                Investigating Officer (IO)
              </Typography>
            </Box>
          </Box>
        </Box>
      </Toolbar>
    </AppBar>
  );
}

==> ./src/components/PDFPreviewModal.jsx <==
import React from 'react';
import { Dialog, DialogTitle, DialogContent, DialogActions, Button, Box, Typography, IconButton } from '@mui/material';
import { Close, Download, Description } from '@mui/icons-material';

export default function PDFPreviewModal({ open, onClose, pdfUrl, title = 'Section 94 BNSS Legal Notice' }) {
  if (!open) return null;

  return (
    <Dialog open={open} onClose={onClose} maxWidth="md" fullWidth PaperProps={{ className: 'glass-card', sx: { bgcolor: '#111827' } }}>
      <DialogTitle sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '1px solid rgba(255, 255, 255, 0.08)' }}>
        <Box sx={{ display: 'flex', alignItems: 'center', gap: 1 }}>
          <Description sx={{ color: '#2563EB' }} />
          <Typography variant="h6" sx={{ fontWeight: 700 }}>
            {title}
          </Typography>
        </Box>
        <IconButton onClick={onClose} sx={{ color: '#9CA3AF' }}>
          <Close />
        </IconButton>
      </DialogTitle>

      <DialogContent sx={{ p: 3, textAlign: 'center' }}>
        <Box
          sx={{
            p: 4,
            borderRadius: 2,
            background: 'rgba(255, 255, 255, 0.02)',
            border: '1px dashed rgba(255, 255, 255, 0.15)',
            my: 2
          }}
        >
          <Description sx={{ fontSize: 64, color: '#2563EB', mb: 2 }} />
          <Typography variant="h6" sx={{ fontWeight: 700, mb: 1 }}>
            Official Legal Requisition PDF Document Generated
          </Typography>
          <Typography variant="body2" sx={{ color: '#9CA3AF', mb: 3 }}>
            Formatted under Section 94 BNSS, 2023 with digital seal of Station House Officer.
          </Typography>

          <Button
            variant="contained"
            color="primary"
            startIcon={<Download />}
            href={`http://localhost:4000${pdfUrl}`}
            target="_blank"
            sx={{ px: 4, py: 1.5, fontWeight: 700 }}
          >
            Download Turnkey PDF Notice
          </Button>
        </Box>
      </DialogContent>

      <DialogActions sx={{ p: 2, borderTop: '1px solid rgba(255, 255, 255, 0.08)' }}>
        <Button onClick={onClose} sx={{ color: '#9CA3AF' }}>
          Close Preview
        </Button>
      </DialogActions>
    </Dialog>
  );
}

==> ./src/components/Sidebar.jsx <==
import React from 'react';
import { useNavigate, useLocation } from 'react-router-dom';
import { Box, List, ListItem, ListItemButton, ListItemIcon, ListItemText, Divider } from '@mui/material';
import {
  Dashboard,
  CloudUpload,
  AutoAwesome,
  Description,
  QueryStats,
  AdminPanelSettings,
  Logout
} from '@mui/icons-material';

const menuItems = [
  { text: 'Dashboard', icon: <Dashboard />, path: '/' },
  { text: 'Multimodal Ingestion', icon: <CloudUpload />, path: '/complaints' },
  { text: 'Agentic Investigation', icon: <AutoAwesome />, path: '/investigation' },
  { text: 'Legal Request Tracker', icon: <Description />, path: '/requests' },
  { text: 'Analytics & Link Graph', icon: <QueryStats />, path: '/analytics' },
  { text: 'Admin & Audit Trail', icon: <AdminPanelSettings />, path: '/admin' }
];

export default function Sidebar() {
  const navigate = useNavigate();
  const location = useLocation();

  return (
    <Box sx={{ width: 240, background: '#111827', borderRight: '1px solid rgba(255, 255, 255, 0.08)', minHeight: 'calc(100vh - 64px)', p: 1.5 }}>
      <List>
        {menuItems.map((item) => {
          const isActive = location.pathname === item.path;
          return (
            <ListItem key={item.text} disablePadding sx={{ mb: 0.5 }}>
              <ListItemButton
                onClick={() => navigate(item.path)}
                sx={{
                  borderRadius: 2,
                  bgcolor: isActive ? 'rgba(37, 99, 235, 0.15)' : 'transparent',
                  color: isActive ? '#60A5FA' : '#9CA3AF',
                  border: isActive ? '1px solid rgba(37, 99, 235, 0.3)' : '1px solid transparent',
                  '&:hover': {
                    bgcolor: 'rgba(37, 99, 235, 0.1)',
                    color: '#F9FAFB'
                  }
                }}
              >
                <ListItemIcon sx={{ color: isActive ? '#60A5FA' : '#9CA3AF', minWidth: 40 }}>
                  {item.icon}
                </ListItemIcon>
                <ListItemText primary={item.text} primaryTypographyProps={{ fontSize: '0.9rem', fontWeight: isActive ? 700 : 500 }} />
              </ListItemButton>
            </ListItem>
          );
        })}
      </List>

      <Divider sx={{ my: 2, borderColor: 'rgba(255, 255, 255, 0.08)' }} />

      <List>
        <ListItem disablePadding>
          <ListItemButton onClick={() => navigate('/login')} sx={{ borderRadius: 2, color: '#EF4444' }}>
            <ListItemIcon sx={{ color: '#EF4444', minWidth: 40 }}>
              <Logout />
            </ListItemIcon>
            <ListItemText primary="Sign Out" primaryTypographyProps={{ fontSize: '0.9rem', fontWeight: 600 }} />
          </ListItemButton>
        </ListItem>
      </List>
    </Box>
  );
}

==> ./src/index.css <==
:root {
  --bg-dark: #0A0E1A;
  --bg-card: rgba(17, 24, 39, 0.85);
  --border-card: rgba(255, 255, 255, 0.08);
  --primary-blue: #0D47A1;
  --accent-blue: #2563EB;
  --accent-green: #10B981;
  --accent-amber: #F59E0B;
  --accent-red: #EF4444;
  --text-main: #F9FAFB;
  --text-muted: #9CA3AF;
  --font-family: 'Inter', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}

body {
  margin: 0;
  padding: 0;
  font-family: var(--font-family);
  background-color: var(--bg-dark);
  color: var(--text-main);
  -webkit-font-smoothing: antialiased;
}

/* Glassmorphism Cards */
.glass-card {
  background: var(--bg-card);
  backdrop-filter: blur(12px);
  border: 1px solid var(--border-card);
  border-radius: 12px;
  box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
}

.glass-card-hover {
  transition: all 0.25s ease-in-out;
}
.glass-card-hover:hover {
  border-color: rgba(37, 99, 235, 0.4);
  transform: translateY(-2px);
  box-shadow: 0 12px 40px 0 rgba(37, 99, 235, 0.15);
}

/* Glowing Badges */
.badge-cyber {
  background: rgba(37, 99, 235, 0.15);
  color: #60A5FA;
  border: 1px solid rgba(37, 99, 235, 0.4);
}

.badge-conventional {
  background: rgba(245, 158, 11, 0.15);
  color: #FBBF24;
  border: 1px solid rgba(245, 158, 11, 0.4);
}

.badge-success {
  background: rgba(16, 185, 129, 0.15);
  color: #34D399;
  border: 1px solid rgba(16, 185, 129, 0.4);
}

/* Custom Scrollbar */
::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}
::-webkit-scrollbar-track {
  background: #0A0E1A;
}
::-webkit-scrollbar-thumb {
  background: #1F2937;
  border-radius: 4px;
}
::-webkit-scrollbar-thumb:hover {
  background: #374151;
}

==> ./src/main.jsx <==
import React from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import { ThemeProvider, CssBaseline } from '@mui/material';
import theme from './theme/theme';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ThemeProvider theme={theme}>
      <CssBaseline />
      <BrowserRouter>
        <App />
      </BrowserRouter>
    </ThemeProvider>
  </React.StrictMode>
);

==> ./src/pages/AdminPage.jsx <==
import React from 'react';
import { Box, Paper, Typography, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Chip, Grid } from '@mui/material';
import { AdminPanelSettings, Security, History } from '@mui/icons-material';

export default function AdminPage() {
  const roles = [
    { role: 'IO (Investigating Officer)', perm: 'Ingest complaints, Run Agent Studio, Generate Legal Requests', count: 18 },
    { role: 'SHO (Station House Officer)', perm: 'Approve & Dispatch Legal Requests, View Station Audit Logs', count: 4 },
    { role: 'Legal Advisor', perm: 'Review BNS/BSA grounding, Validate Section 94 notices', count: 2 },
    { role: 'System Admin', perm: 'Full RBAC, Vector store re-indexing, System Configuration', count: 1 }
  ];

  const auditLogs = [
    { time: '12:44:10', user: 'PSI V. K. Patel (IO)', action: 'EXECUTE_LANGGRAPH_STUDIO', case_no: 'CR-2026-9910' },
    { time: '12:45:02', user: 'PI R. S. Sharma (SHO)', action: 'APPROVE_SECTION_94_BNSS_NOTICE', case_no: 'CR-2026-9910' },
    { time: '12:46:18', user: 'PSI V. K. Patel (IO)', action: 'DISPATCH_LEGAL_NOTICE_EMAIL', case_no: 'CR-2026-9910' }
  ];

  return (
    <Box sx={{ p: 3 }}>
      <Box sx={{ mb: 3 }}>
        <Typography variant="h5" sx={{ fontWeight: 800, color: '#F9FAFB' }}>
          Role-Based Access Control (RBAC) & Audit Trail
        </Typography>
        <Typography variant="body2" sx={{ color: '#9CA3AF' }}>
          Enforces strict role permissions for IO, SHO, and Legal Advisors with immutable audit logs.
        </Typography>
      </Box>

      <Grid container spacing={3}>
        <Grid item xs={12} md={6}>
          <Paper className="glass-card" sx={{ p: 3 }}>
            <Typography variant="h6" sx={{ fontWeight: 700, mb: 2, display: 'flex', alignItems: 'center', gap: 1 }}>
              <Security sx={{ color: '#2563EB' }} /> Active Role Permission Matrix
            </Typography>

            {roles.map((r, i) => (
              <Box key={i} sx={{ p: 2, borderRadius: 2, bg: 'rgba(255,255,255,0.02)', border: '1px solid rgba(255,255,255,0.08)', mb: 1.5 }}>
                <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', mb: 0.5 }}>
                  <Typography variant="subtitle2" sx={{ fontWeight: 700, color: '#F9FAFB' }}>
                    {r.role}
                  </Typography>
                  <Chip label={`${r.count} Active`} size="small" className="badge-cyber" />
                </Box>
                <Typography variant="caption" sx={{ color: '#9CA3AF' }}>
                  {r.perm}
                </Typography>
              </Box>
            ))}
          </Paper>
        </Grid>

        <Grid item xs={12} md={6}>
          <Paper className="glass-card" sx={{ p: 3 }}>
            <Typography variant="h6" sx={{ fontWeight: 700, mb: 2, display: 'flex', alignItems: 'center', gap: 1 }}>
              <History sx={{ color: '#10B981' }} /> Real-time System Audit Trail
            </Typography>

            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Time</TableCell>
                    <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Officer User</TableCell>
                    <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Action</TableCell>
                    <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Case ID</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {auditLogs.map((log, i) => (
                    <TableRow key={i}>
                      <TableCell sx={{ color: '#9CA3AF', fontSize: '0.8rem' }}>{log.time}</TableCell>
                      <TableCell sx={{ color: '#F9FAFB', fontWeight: 600, fontSize: '0.85rem' }}>{log.user}</TableCell>
                      <TableCell><Chip label={log.action} size="small" variant="outlined" sx={{ borderColor: 'rgba(255,255,255,0.2)', fontSize: '0.7rem' }} /></TableCell>
                      <TableCell sx={{ color: '#60A5FA', fontWeight: 700, fontSize: '0.85rem' }}>{log.case_no}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          </Paper>
        </Grid>
      </Grid>
    </Box>
  );
}

==> ./src/pages/AnalyticsPage.jsx <==
import React from 'react';
import { Box, Paper, Typography, Grid } from '@mui/material';
import { QueryStats, Timeline, Hub } from '@mui/icons-material';
import { ResponsiveContainer, BarChart, Bar, XAxis, YAxis, Tooltip, CartesianGrid } from 'recharts';
import LinkAnalysisGraph from '../components/LinkAnalysisGraph';

const cdrData = [
  { time: '00:00', calls: 2 },
  { time: '04:00', calls: 0 },
  { time: '08:00', calls: 5 },
  { time: '12:00', calls: 24 },
  { time: '16:00', calls: 38 },
  { time: '20:00', calls: 14 }
];

export default function AnalyticsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Box sx={{ mb: 3 }}>
        <Typography variant="h5" sx={{ fontWeight: 800, color: '#F9FAFB' }}>
          Response Analytics & Serial Link Engine
        </Typography>
        <Typography variant="body2" sx={{ color: '#9CA3AF' }}>
          Analyzes CDR frequency distribution, cell tower movements, and cross-case offender networks.
        </Typography>
      </Box>

      <LinkAnalysisGraph />

      <Grid container spacing={3}>
        <Grid item xs={12} md={8}>
          <Paper className="glass-card" sx={{ p: 3 }}>
            <Typography variant="h6" sx={{ fontWeight: 700, mb: 2, display: 'flex', alignItems: 'center', gap: 1 }}>
              <Timeline sx={{ color: '#2563EB' }} /> CDR Call Frequency Timeline (Target: +91 98765 43210)
            </Typography>

            <Box sx={{ width: '100%', height: 300 }}>
              <ResponsiveContainer>
                <BarChart data={cdrData}>
                  <CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,0.08)" />
                  <XAxis dataKey="time" stroke="#9CA3AF" />
                  <YAxis stroke="#9CA3AF" />
                  <Tooltip contentStyle={{ backgroundColor: '#111827', borderColor: '#2563EB' }} />
                  <Bar dataKey="calls" fill="#2563EB" radius={[4, 4, 0, 0]} />
                </BarChart>
              </ResponsiveContainer>
            </Box>
          </Paper>
        </Grid>

        <Grid item xs={12} md={4}>
          <Paper className="glass-card" sx={{ p: 3, height: '100%' }}>
            <Typography variant="h6" sx={{ fontWeight: 700, mb: 2, display: 'flex', alignItems: 'center', gap: 1 }}>
              <Hub sx={{ color: '#10B981' }} /> Intelligence Insights
            </Typography>

            <Box sx={{ p: 2, borderRadius: 2, bg: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.08)', mb: 2 }}>
              <Typography variant="subtitle2" sx={{ fontWeight: 700, color: '#34D399' }}>
                Peak Activity Window
              </Typography>
              <Typography variant="caption" sx={{ color: '#9CA3AF' }}>
                Highest call volume detected between 14:00 and 17:00 (38 outgoing calls).
              </Typography>
            </Box>

            <Box sx={{ p: 2, borderRadius: 2, bg: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.08)' }}>
              <Typography variant="subtitle2" sx={{ fontWeight: 700, color: '#60A5FA' }}>
                Primary Cell Tower Location
              </Typography>
              <Typography variant="caption" sx={{ color: '#9CA3AF' }}>
                Surat CG Road Tower (AHM-CG-TW-42) - 82% anchor score.
              </Typography>
            </Box>
          </Paper>
        </Grid>
      </Grid>
    </Box>
  );
}

==> ./src/pages/ComplaintsPage.jsx <==
import React, { useState } from 'react';
import { Box, Paper, Typography, Grid, TextField, Button, MenuItem, Select, FormControl, InputLabel, CircularProgress, Chip, Alert, Card, CardContent } from '@mui/material';
import { CloudUpload, Mic, PictureAsPdf, Image, Translate, CheckCircle, ArrowForward } from '@mui/icons-material';
import { useNavigate } from 'react-router-dom';
import axios from 'axios';

export default function ComplaintsPage() {
  const navigate = useNavigate();
  const [inputType, setInputType] = useState('text');
  const [language, setLanguage] = useState('gu');
  const [rawText, setRawText] = useState('મને વોટ્સએપ પર લોન લિંક મોકલીને ₹85,000 નું UPI ફ્રોડ કર્યું છે. મોબાઈલ નંબર 9876543210 અને UPI ID scammer@paytm છે.');
  const [loading, setLoading] = useState(false);
  const [extractedResult, setExtractedResult] = useState(null);

  const handleIngest = async () => {
    setLoading(true);
    try {
      const response = await axios.post('http://localhost:4000/api/complaints/upload', {
        input_type: inputType,
        raw_text: rawText,
        original_language: language
      });
      setExtractedResult(response.data);
    } catch (err) {
      console.error(err);
    } finally {
      setLoading(false);
    }
  };

  return (
    <Box sx={{ p: 3 }}>
      <Box sx={{ mb: 3 }}>
        <Typography variant="h5" sx={{ fontWeight: 800, color: '#F9FAFB' }}>
          Multimodal Complaint Ingestion Engine
        </Typography>
        <Typography variant="body2" sx={{ color: '#9CA3AF' }}>
          Ingests Gujarati, Hindi, and English complaints from PDFs, audio recordings, scanned handwriting, or raw text.
        </Typography>
      </Box>

      <Grid container spacing={3}>
        {/* Left Column: Form Controls */}
        <Grid item xs={12} md={6}>
          <Paper className="glass-card" sx={{ p: 3 }}>
            <Typography variant="h6" sx={{ fontWeight: 700, mb: 2 }}>
              Complaint Submission & Source Selection
            </Typography>

            <Grid container spacing= {2} sx={{ mb: 2 }}>
              <Grid item xs={6}>
                <FormControl fullWidth size="small">
                  <InputLabel>Input Format</InputLabel>
                  <Select value={inputType} label="Input Format" onChange={(e) => setInputType(e.target.value)}>
                    <MenuItem value="text">Direct Text Entry</MenuItem>
                    <MenuItem value="pdf">PDF File Upload</MenuItem>
                    <MenuItem value="audio">Audio Recording (.wav / .mp3)</MenuItem>
                    <MenuItem value="image">Scanned FIR / Image</MenuItem>
                  </Select>
                </FormControl>
              </Grid>

              <Grid item xs={6}>
                <FormControl fullWidth size="small">
                  <InputLabel>Original Language</InputLabel>
                  <Select value={language} label="Original Language" onChange={(e) => setLanguage(e.target.value)}>
                    <MenuItem value="gu">Gujarati (ગુજરાતી)</MenuItem>
                    <MenuItem value="hi">Hindi (हिंदी)</MenuItem>
                    <MenuItem value="en">English</MenuItem>
                  </Select>
                </FormControl>
              </Grid>
            </Grid>

            {inputType === 'text' ? (
              <TextField
                multiline
                rows={6}
                fullWidth
                label="Complaint Statement Text"
                value={rawText}
                onChange={(e) => setRawText(e.target.value)}
                sx={{ mb: 3 }}
              />
            ) : (
              <Box
                sx={{
                  p: 4,
                  border: '2px dashed rgba(37, 99, 235, 0.4)',
                  borderRadius: 2,
                  textAlign: 'center',
                  background: 'rgba(37, 99, 235, 0.05)',
                  mb: 3,
                  cursor: 'pointer'
                }}
              >
                <CloudUpload sx={{ fontSize: 48, color: '#2563EB', mb: 1 }} />
                <Typography variant="body1" sx={{ fontWeight: 700 }}>
                  Click to select {inputType.toUpperCase()} file
                </Typography>
                <Typography variant="caption" sx={{ color: '#9CA3AF' }}>
                  Supports PDF documents, WAV/MP3 audio, JPEG/PNG images
                </Typography>
              </Box>
            )}

            <Button
              variant="contained"
              color="primary"
              fullWidth
              size="large"
              onClick={handleIngest}
              disabled={loading}
              startIcon={loading ? <CircularProgress size={20} color="inherit" /> : <Translate />}
              sx={{ py: 1.5, fontWeight: 700 }}
            >
              {loading ? 'Processing Ingestion Pipeline...' : 'Run Multimodal Extraction Engine'}
            </Button>
          </Paper>
        </Grid>

        {/* Right Column: Structured Extraction Result */}
        <Grid item xs={12} md={6}>
          <Paper className="glass-card" sx={{ p: 3, minHeight: 420 }}>
            <Typography variant="h6" sx={{ fontWeight: 700, mb: 2, display: 'flex', alignItems: 'center', gap: 1 }}>
              <CheckCircle sx={{ color: '#10B981' }} /> Structured Extraction & Legal Entity Result
            </Typography>

            {extractedResult ? (
              <Box>
                <Alert severity="success" sx={{ mb: 2, bgcolor: 'rgba(16, 185, 129, 0.1)', color: '#34D399' }}>
                  Language Detected: <b>{extractedResult.original_language?.toUpperCase()}</b> | Severity Score: <b>{extractedResult.severity_score} / 10</b>
                </Alert>

                <Box sx={{ mb: 2 }}>
                  <Typography variant="caption" sx={{ color: '#9CA3AF', fontWeight: 700 }}>ENGLISH TRANSLATION</Typography>
                  <Typography variant="body2" sx={{ color: '#F9FAFB', bgcolor: 'rgba(255,255,255,0.03)', p: 1.5, borderRadius: 1.5, border: '1px solid rgba(255,255,255,0.08)' }}>
                    {extractedResult.translated_text}
                  </Typography>
                </Box>

                <Typography variant="caption" sx={{ color: '#9CA3AF', fontWeight: 700, mb: 1, display: 'block' }}>EXTRACTED ENTITIES</Typography>
                <Grid container spacing={1} sx={{ mb: 2 }}>
                  {extractedResult.entities?.phone_numbers?.map((p, i) => (
                    <Grid item key={i}><Chip label={`Phone: ${p}`} size="small" color="primary" variant="outlined" /></Grid>
                  ))}
                  {extractedResult.entities?.vpas_upis?.map((v, i) => (
                    <Grid item key={i}><Chip label={`VPA: ${v}`} size="small" color="warning" variant="outlined" /></Grid>
                  ))}
                  {extractedResult.entities?.bank_accounts?.map((b, i) => (
                    <Grid item key={i}><Chip label={`A/C: ${b}`} size="small" color="secondary" variant="outlined" /></Grid>
                  ))}
                  <Grid item><Chip label={`Loss: ₹${extractedResult.entities?.monetary_loss || 85000}`} size="small" color="error" /></Grid>
                </Grid>

                <Typography variant="caption" sx={{ color: '#9CA3AF', fontWeight: 700, mb: 1, display: 'block' }}>SUGGESTED BNS / IT ACT SECTIONS</Typography>
                <Box sx={{ display: 'flex', gap: 1, mb: 3 }}>
                  {extractedResult.suggested_bns_sections?.map((s, i) => (
                    <Chip key={i} label={s} sx={{ bgcolor: 'rgba(37, 99, 235, 0.2)', color: '#60A5FA', fontWeight: 700 }} />
                  ))}
                </Box>

                <Button
                  variant="contained"
                  color="secondary"
                  fullWidth
                  endIcon={<ArrowForward />}
                  onClick={() => navigate('/investigation')}
                  sx={{ py: 1.2, fontWeight: 700 }}
                >
                  Create Case & Launch Agentic Studio
                </Button>
              </Box>
            ) : (
              <Box sx={{ textAlign: 'center', color: '#9CA3AF', pt: 8 }}>
                <CloudUpload sx={{ fontSize: 64, opacity: 0.3, mb: 1 }} />
                <Typography variant="body1">
                  Submit a complaint to trigger automated translation, entity extraction, and BNS section grounding.
                </Typography>
              </Box>
            )}
          </Paper>
        </Grid>
      </Grid>
    </Box>
  );
}

==> ./src/pages/DashboardPage.jsx <==
import React, { useState, useEffect } from 'react';
import { Box, Grid, Paper, Typography, Button, Card, CardContent, Chip, Table, TableBody, TableCell, TableContainer, TableHead, TableRow } from '@mui/material';
import { Security, CloudUpload, AutoAwesome, Description, TrendingUp, Warning, CheckCircle, ArrowForward } from '@mui/icons-material';
import { useNavigate } from 'react-router-dom';
import axios from 'axios';

export default function DashboardPage() {
  const navigate = useNavigate();
  const [stats, setStats] = useState({
    total_cases: 42,
    active_cyber_cases: 28,
    active_conventional_cases: 14,
    legal_requests_dispatched: 89,
    serial_offenders_linked: 11
  });

  const recentCases = [
    { id: 'CR-2026-9910', fir: 'FIR-9910/2026', type: 'CYBER', sub: 'UPI Financial Fraud', status: 'INVESTIGATING', io: 'PSI V. K. Patel', date: 'Today, 10:30 AM' },
    { id: 'CR-2026-8812', fir: 'FIR-8812/2026', type: 'CONVENTIONAL', sub: 'Housebreaking & Theft', status: 'CHARGESHEET', io: 'PSI V. K. Patel', date: 'Yesterday' },
    { id: 'CR-2026-7734', fir: 'FIR-7734/2026', type: 'CYBER', sub: 'Social Media Impersonation', status: 'REGISTERED', io: 'PSI V. K. Patel', date: '20 Jul 2026' }
  ];

  return (
    <Box sx={{ p: 3 }}>
      {/* Header */}
      <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', mb: 3 }}>
        <Box>
          <Typography variant="h5" sx={{ fontWeight: 800, color: '#F9FAFB' }}>
            Investigation Command Center
          </Typography>
          <Typography variant="body2" sx={{ color: '#9CA3AF' }}>
            Real-time intelligence-led case metrics and agentic AI workflows.
          </Typography>
        </Box>
        <Button
          variant="contained"
          color="primary"
          startIcon={<CloudUpload />}
          onClick={() => navigate('/complaints')}
          sx={{ py: 1.2, px: 3, fontWeight: 700 }}
        >
          Ingest New Complaint
        </Button>
      </Box>

      {/* Stats Cards */}
      <Grid container spacing={2.5} sx={{ mb: 4 }}>
        <Grid item xs={12} sm={6} md={3}>
          <Paper className="glass-card glass-card-hover" sx={{ p: 2.5 }}>
            <Typography variant="caption" sx={{ color: '#9CA3AF', fontWeight: 600 }}>ACTIVE INVESTIGATIONS</Typography>
            <Typography variant="h4" sx={{ fontWeight: 800, color: '#F9FAFB', my: 0.5 }}>{stats.total_cases}</Typography>
            <Box sx={{ display: 'flex', gap: 1 }}>
              <Chip label={`${stats.active_cyber_cases} Cyber`} size="small" className="badge-cyber" />
              <Chip label={`${stats.active_conventional_cases} Field`} size="small" className="badge-conventional" />
            </Box>
          </Paper>
        </Grid>

        <Grid item xs={12} sm={6} md={3}>
          <Paper className="glass-card glass-card-hover" sx={{ p: 2.5 }}>
            <Typography variant="caption" sx={{ color: '#9CA3AF', fontWeight: 600 }}>LEGAL NOTICES DISPATCHED</Typography>
            <Typography variant="h4" sx={{ fontWeight: 800, color: '#60A5FA', my: 0.5 }}>{stats.legal_requests_dispatched}</Typography>
            <Typography variant="caption" sx={{ color: '#10B981', display: 'flex', alignItems: 'center', gap: 0.5 }}>
              <CheckCircle fontSize="inherit" /> Section 94 BNSS Auto-PDFs
            </Typography>
          </Paper>
        </Grid>

        <Grid item xs={12} sm={6} md={3}>
          <Paper className="glass-card glass-card-hover" sx={{ p: 2.5 }}>
            <Typography variant="caption" sx={{ color: '#9CA3AF', fontWeight: 600 }}>SERIAL OFFENDERS LINKED</Typography>
            <Typography variant="h4" sx={{ fontWeight: 800, color: '#FBBF24', my: 0.5 }}>{stats.serial_offenders_linked}</Typography>
            <Typography variant="caption" sx={{ color: '#F59E0B', display: 'flex', alignItems: 'center', gap: 0.5 }}>
              <Warning fontSize="inherit" /> Qdrant VPA & Phone Matches
            </Typography>
          </Paper>
        </Grid>

        <Grid item xs={12} sm={6} md={3}>
          <Paper className="glass-card glass-card-hover" sx={{ p: 2.5 }}>
            <Typography variant="caption" sx={{ color: '#9CA3AF', fontWeight: 600 }}>AVG. PLAN GENERATION</Typography>
            <Typography variant="h4" sx={{ fontWeight: 800, color: '#34D399', my: 0.5 }}>1.4s</Typography>
            <Typography variant="caption" sx={{ color: '#9CA3AF' }}>LangGraph Parallel Pods</Typography>
          </Paper>
        </Grid>
      </Grid>

      {/* Quick Launchpad & Active Cases */}
      <Grid container spacing={3}>
        <Grid item xs={12} md={8}>
          <Paper className="glass-card" sx={{ p: 3 }}>
            <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', mb: 2 }}>
              <Typography variant="h6" sx={{ fontWeight: 700 }}>
                Recent Active Case Investigations
              </Typography>
              <Button size="small" endIcon={<ArrowForward />} onClick={() => navigate('/investigation')}>
                View All Studio
              </Button>
            </Box>

            <TableContainer>
              <Table>
                <TableHead>
                  <TableRow>
                    <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Case No.</TableCell>
                    <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Category</TableCell>
                    <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Sub-Type</TableCell>
                    <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Assigned IO</TableCell>
                    <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Action</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {recentCases.map((row) => (
                    <TableRow key={row.id} hover sx={{ '&:last-child td, &:last-child th': { border: 0 } }}>
                      <TableCell sx={{ fontWeight: 700, color: '#F9FAFB' }}>{row.id}<br/><Typography variant="caption" sx={{ color: '#9CA3AF' }}>{row.fir}</Typography></TableCell>
                      <TableCell>
                        <Chip label={row.type} size="small" className={row.type === 'CYBER' ? 'badge-cyber' : 'badge-conventional'} />
                      </TableCell>
                      <TableCell sx={{ color: '#E5E7EB' }}>{row.sub}</TableCell>
                      <TableCell sx={{ color: '#9CA3AF' }}>{row.io}</TableCell>
                      <TableCell>
                        <Button
                          variant="outlined"
                          size="small"
                          startIcon={<AutoAwesome />}
                          onClick={() => navigate('/investigation')}
                        >
                          Studio
                        </Button>
                      </TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          </Paper>
        </Grid>

        {/* Quick Launchpad */}
        <Grid item xs={12} md={4}>
          <Paper className="glass-card" sx={{ p: 3, h: '100%' }}>
            <Typography variant="h6" sx={{ fontWeight: 700, mb: 2 }}>
              Agentic Action Launchpad
            </Typography>

            <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
              <Button
                variant="contained"
                sx={{ py: 1.5, justifyContent: 'flex-start', bgcolor: 'rgba(37, 99, 235, 0.2)', color: '#60A5FA', border: '1px solid rgba(37, 99, 235, 0.4)' }}
                startIcon={<CloudUpload />}
                onClick={() => navigate('/complaints')}
              >
                Multimodal Gujarati/Hindi Ingestion
              </Button>

              <Button
                variant="contained"
                sx={{ py: 1.5, justifyContent: 'flex-start', bgcolor: 'rgba(16, 185, 129, 0.2)', color: '#34D399', border: '1px solid rgba(16, 185, 129, 0.4)' }}
                startIcon={<AutoAwesome />}
                onClick={() => navigate('/investigation')}
              >
                Trigger LangGraph Agent Studio
              </Button>

              <Button
                variant="contained"
                sx={{ py: 1.5, justifyContent: 'flex-start', bgcolor: 'rgba(245, 158, 11, 0.2)', color: '#FBBF24', border: '1px solid rgba(245, 158, 11, 0.4)' }}
                startIcon={<Description />}
                onClick={() => navigate('/requests')}
              >
                Turnkey Section 94 BNSS PDFs
              </Button>
            </Box>
          </Paper>
        </Grid>
      </Grid>
    </Box>
  );
}

==> ./src/pages/InvestigationPage.jsx <==
import React, { useState } from 'react';
import { Box, Paper, Typography, Grid, Button, Card, CardContent, Chip, Accordion, AccordionSummary, AccordionDetails, Alert, CircularProgress } from '@mui/material';
import { AutoAwesome, ExpandMore, CheckCircle, Description, Shield, Gavel, LocalPolice, Download } from '@mui/icons-material';
import AgentFlowGraph from '../components/AgentFlowGraph';
import LinkAnalysisGraph from '../components/LinkAnalysisGraph';
import PDFPreviewModal from '../components/PDFPreviewModal';
import axios from 'axios';

export default function InvestigationPage() {
  const [loading, setLoading] = useState(false);
  const [investigationData, setInvestigationData] = useState(null);
  const [pdfModalOpen, setPdfModalOpen] = useState(false);

  const handleRunAgentStudio = async () => {
    setLoading(true);
    try {
      const res = await axios.post('http://localhost:4000/api/cases/CR-2026-9910/investigate', {
        case_number: 'CR-2026-9910',
        complaint_text: 'Defrauded of INR 85,000 via WhatsApp UPI loan link scammer@paytm.',
        crime_category: 'CYBER'
      });
      setInvestigationData(res.data);
    } catch (err) {
      console.error(err);
    } finally {
      setLoading(false);
    }
  };

  return (
    <Box sx={{ p: 3 }}>
      {/* Header */}
      <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', mb: 3 }}>
        <Box>
          <Typography variant="h5" sx={{ fontWeight: 800, color: '#F9FAFB' }}>
            Agentic Investigation Studio
          </Typography>
          <Typography variant="body2" sx={{ color: '#9CA3AF' }}>
            Case No: <b>CR-2026-9910</b> | FIR No: <b>FIR-9910/2026</b> | Assigned IO: <b>PSI V. K. Patel</b>
          </Typography>
        </Box>
        <Button
          variant="contained"
          color="primary"
          startIcon={loading ? <CircularProgress size={20} color="inherit" /> : <AutoAwesome />}
          onClick={handleRunAgentStudio}
          disabled={loading}
          sx={{ py: 1.2, px: 3, fontWeight: 700 }}
        >
          {loading ? 'Running Specialist Agents...' : 'Execute LangGraph Agent Studio'}
        </Button>
      </Box>

      {/* Visual LangGraph Execution Graph */}
      <AgentFlowGraph activeStep={investigationData ? 6 : 4} status="APPROVED" />

      {/* Visual Link Analysis Graph */}
      <LinkAnalysisGraph matches={investigationData?.cross_case_matches} />

      {/* Main Grid: Steps & Legal Grounding */}
      <Grid container spacing={3}>
        {/* Left: Step-by-Step Guidance */}
        <Grid item xs={12} md={7}>
          <Paper className="glass-card" sx={{ p: 3 }}>
            <Typography variant="h6" sx={{ fontWeight: 700, mb: 2, display: 'flex', alignItems: 'center', gap: 1 }}>
              <LocalPolice sx={{ color: '#2563EB' }} /> SOP-Grounded Step-by-Step Investigation Path
            </Typography>

            {investigationData ? (
              <Box>
                {investigationData.investigation_steps?.map((step, idx) => (
                  <Accordion key={idx} defaultExpanded sx={{ background: 'rgba(255,255,255,0.02)', mb: 1.5, border: '1px solid rgba(255,255,255,0.08)' }}>
                    <AccordionSummary expandIcon={<ExpandMore />}>
                      <Box sx={{ display: 'flex', alignItems: 'center', gap: 1.5 }}>
                        <Chip label={`Step ${step.step_number}`} size="small" color="primary" sx={{ fontWeight: 700 }} />
                        <Typography variant="subtitle2" sx={{ fontWeight: 700, color: '#F9FAFB' }}>
                          {step.title}
                        </Typography>
                      </Box>
                    </AccordionSummary>
                    <AccordionDetails>
                      <Typography variant="body2" sx={{ color: '#9CA3AF', mb: 1.5 }}>
                        {step.description}
                      </Typography>
                      <Chip label={`Ref: ${step.sop_reference}`} size="small" variant="outlined" sx={{ borderColor: 'rgba(255,255,255,0.2)', color: '#9CA3AF' }} />
                    </AccordionDetails>
                  </Accordion>
                ))}

                <Alert severity="success" icon={<CheckCircle fontSize="inherit" />} sx={{ mt: 2, bgcolor: 'rgba(16, 185, 129, 0.1)', color: '#34D399' }}>
                  HITL Approved by Investigating Officer V. K. Patel. Master FIR compiled.
                </Alert>
              </Box>
            ) : (
              <Box sx={{ textAlign: 'center', color: '#9CA3AF', py: 6 }}>
                <AutoAwesome sx={{ fontSize: 48, opacity: 0.3, mb: 1 }} />
                <Typography variant="body1">
                  Click 'Execute LangGraph Agent Studio' to generate SOP-grounded investigation steps.
                </Typography>
              </Box>
            )}
          </Paper>
        </Grid>

        {/* Right: Statutory Grounding & Turnkey PDF Notices */}
        <Grid item xs={12} md={5}>
          <Paper className="glass-card" sx={{ p: 3, mb: 3 }}>
            <Typography variant="h6" sx={{ fontWeight: 700, mb: 2, display: 'flex', alignItems: 'center', gap: 1 }}>
              <Gavel sx={{ color: '#F59E0B' }} /> Grounded Statutory Penal Sections
            </Typography>

            <Box sx={{ p: 2, borderRadius: 2, background: 'rgba(37, 99, 235, 0.08)', border: '1px solid rgba(37, 99, 235, 0.3)', mb: 2 }}>
              <Typography variant="subtitle2" sx={{ fontWeight: 700, color: '#60A5FA' }}>
                BNS Section 318(4) & IT Act Section 66D
              </Typography>
              <Typography variant="caption" sx={{ color: '#9CA3AF', display: 'block', mt: 0.5 }}>
                Punishment for cheating by personation using computer resource. Cognizable & Non-Bailable.
              </Typography>
            </Box>

            <Box sx={{ p: 2, borderRadius: 2, background: 'rgba(16, 185, 129, 0.08)', border: '1px solid rgba(16, 185, 129, 0.3)' }}>
              <Typography variant="subtitle2" sx={{ fontWeight: 700, color: '#34D399' }}>
                Section 63 Bharatiya Sakshya Adhiniyam (BSA), 2023
              </Typography>
              <Typography variant="caption" sx={{ color: '#9CA3AF', display: 'block', mt: 0.5 }}>
                Mandatory electronic evidence certificate required for WhatsApp screenshots and CDR logs.
              </Typography>
            </Box>
          </Paper>

          {/* Turnkey PDF Notice Box */}
          <Paper className="glass-card" sx={{ p: 3 }}>
            <Typography variant="h6" sx={{ fontWeight: 700, mb: 2, display: 'flex', alignItems: 'center', gap: 1 }}>
              <Description sx={{ color: '#10B981' }} /> Turnkey Legal Requisitions PDF
            </Typography>
            <Typography variant="body2" sx={{ color: '#9CA3AF', mb: 2 }}>
              Section 94 BNSS Legal Notice automatically rendered and ready for dispatch.
            </Typography>

            <Button
              variant="contained"
              color="secondary"
              fullWidth
              startIcon={<Download />}
              onClick={() => setPdfModalOpen(true)}
              sx={{ py: 1.2, fontWeight: 700 }}
            >
              Preview & Download Section 94 BNSS Notice PDF
            </Button>
          </Paper>
        </Grid>
      </Grid>

      {/* PDF Modal */}
      <PDFPreviewModal
        open={pdfModalOpen}
        onClose={() => setPdfModalOpen(false)}
        pdfUrl="/api/requests/download/Notice_Section_94_BNSS_CR-2026-9910.pdf"
      />
    </Box>
  );
}

==> ./src/pages/LoginPage.jsx <==
import React, { useState } from 'react';
import { Box, Paper, Typography, TextField, Button, MenuItem, Select, FormControl, InputLabel } from '@mui/material';
import { Shield, Lock } from '@mui/icons-material';
import { useNavigate } from 'react-router-dom';

export default function LoginPage({ onLogin }) {
  const navigate = useNavigate();
  const [username, setUsername] = useState('io_patel');
  const [password, setPassword] = useState('police123');

  const handleSubmit = (e) => {
    e.preventDefault();
    if (onLogin) onLogin({ username, role: 'IO', full_name: 'PSI Inspector V. K. Patel' });
    navigate('/');
  };

  return (
    <Box
      sx={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: 'radial-gradient(circle at center, #111827 0%, #0A0E1A 100%)',
        p: 2
      }}
    >
      <Paper className="glass-card" sx={{ p: 4, width: '100%', maxWidth: 420, textAlign: 'center' }}>
        <Box sx={{ display: 'inline-flex', p: 2, borderRadius: '50%', background: 'rgba(37, 99, 235, 0.15)', mb: 2 }}>
          <Shield sx={{ fontSize: 44, color: '#2563EB' }} />
        </Box>

        <Typography variant="h5" sx={{ fontWeight: 800, color: '#F9FAFB', mb: 0.5 }}>
          Crime OS AI
        </Typography>
        <Typography variant="body2" sx={{ color: '#9CA3AF', mb: 3 }}>
          State Law Enforcement Portal Access
        </Typography>

        <form onSubmit={handleSubmit}>
          <FormControl fullWidth size="small" sx={{ mb: 2 }}>
            <InputLabel>Select Officer Role Account</InputLabel>
            <Select value={username} label="Select Officer Role Account" onChange={(e) => setUsername(e.target.value)}>
              <MenuItem value="io_patel">PSI Inspector V. K. Patel (Investigating Officer)</MenuItem>
              <MenuItem value="sho_sharma">PI Senior Inspector R. S. Sharma (Station House Officer)</MenuItem>
              <MenuItem value="legal_desai">Adv. A. M. Desai (State Legal Advisor)</MenuItem>
              <MenuItem value="admin_crimeos">System Administrator</MenuItem>
            </Select>
          </FormControl>

          <TextField
            fullWidth
            size="small"
            type="password"
            label="Officer Badge Passcode"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            sx={{ mb: 3 }}
          />

          <Button
            type="submit"
            variant="contained"
            color="primary"
            fullWidth
            size="large"
            startIcon={<Lock />}
            sx={{ py: 1.5, fontWeight: 700 }}
          >
            Authenticate & Access Portal
          </Button>
        </form>
      </Paper>
    </Box>
  );
}

==> ./src/pages/RequestsPage.jsx <==
import React, { useState } from 'react';
import { Box, Paper, Typography, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Button, Chip, Alert, Snackbar } from '@mui/material';
import { Description, Download, Send, CheckCircle, HourglassEmpty } from '@mui/icons-material';
import PDFPreviewModal from '../components/PDFPreviewModal';

export default function RequestsPage() {
  const [pdfModalOpen, setPdfModalOpen] = useState(false);
  const [selectedPdf, setSelectedPdf] = useState('');
  const [toastOpen, setToastOpen] = useState(false);

  const requests = [
    {
      id: 'REQ-2026-001',
      case_no: 'CR-2026-9910',
      type: 'SECTION_94_BNSS',
      provider: 'Reliance Jio Infocomm Ltd.',
      purpose: 'CDR & Tower Location Data Requisition',
      status: 'DISPATCHED',
      pdf_url: '/api/requests/download/Notice_Section_94_BNSS_CR-2026-9910.pdf'
    },
    {
      id: 'REQ-2026-002',
      case_no: 'CR-2026-9910',
      type: 'BANK_FREEZE',
      provider: 'State Bank of India (Cyber Cell)',
      purpose: 'Debit Freeze on Suspect Account 30910293101',
      status: 'APPROVED',
      pdf_url: '/api/requests/download/Notice_Section_94_BNSS_CR-2026-9910.pdf'
    },
    {
      id: 'REQ-2026-003',
      case_no: 'CR-2026-8812',
      type: 'SECTION_94_BNSS',
      provider: 'Bharti Airtel Nodal Office',
      purpose: 'IPDR & Subscriber Detail Requisition',
      status: 'PENDING_APPROVAL',
      pdf_url: '/api/requests/download/Notice_Section_94_BNSS_CR-2026-9910.pdf'
    }
  ];

  const handlePreview = (url) => {
    setSelectedPdf(url);
    setPdfModalOpen(true);
  };

  const handleDispatch = () => {
    setToastOpen(true);
  };

  return (
    <Box sx={{ p: 3 }}>
      <Box sx={{ mb: 3 }}>
        <Typography variant="h5" sx={{ fontWeight: 800, color: '#F9FAFB' }}>
          Automated Legal Request & Dispatch Tracker
        </Typography>
        <Typography variant="body2" sx={{ color: '#9CA3AF' }}>
          Auto-generates Section 94 BNSS notices, LERS CDR requests, and bank freeze orders rendered as turnkey PDFs.
        </Typography>
      </Box>

      <Paper className="glass-card" sx={{ p: 3 }}>
        <TableContainer>
          <Table>
            <TableHead>
              <TableRow>
                <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Request ID</TableCell>
                <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Case No.</TableCell>
                <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Notice Type</TableCell>
                <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Target Service Provider</TableCell>
                <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Approval Status</TableCell>
                <TableCell sx={{ color: '#9CA3AF', fontWeight: 700 }}>Actions</TableCell>
              </TableRow>
            </TableHead>
            <TableBody>
              {requests.map((row) => (
                <TableRow key={row.id} hover>
                  <TableCell sx={{ fontWeight: 700, color: '#F9FAFB' }}>{row.id}</TableCell>
                  <TableCell sx={{ color: '#60A5FA', fontWeight: 700 }}>{row.case_no}</TableCell>
                  <TableCell>
                    <Chip label={row.type} size="small" sx={{ bgcolor: 'rgba(37, 99, 235, 0.2)', color: '#60A5FA', fontWeight: 700 }} />
                  </TableCell>
                  <TableCell sx={{ color: '#E5E7EB' }}>{row.provider}</TableCell>
                  <TableCell>
                    {row.status === 'DISPATCHED' ? (
                      <Chip label="DISPATCHED" size="small" className="badge-success" />
                    ) : row.status === 'APPROVED' ? (
                      <Chip label="APPROVED" size="small" className="badge-cyber" />
                    ) : (
                      <Chip label="PENDING SHO" size="small" className="badge-conventional" />
                    )}
                  </TableCell>
                  <TableCell>
                    <Box sx={{ display: 'flex', gap: 1 }}>
                      <Button
                        variant="outlined"
                        size="small"
                        startIcon={<Description />}
                        onClick={() => handlePreview(row.pdf_url)}
                      >
                        PDF Notice
                      </Button>
                      {row.status === 'APPROVED' && (
                        <Button
                          variant="contained"
                          color="secondary"
                          size="small"
                          startIcon={<Send />}
                          onClick={handleDispatch}
                        >
                          Dispatch
                        </Button>
                      )}
                    </Box>
                  </TableCell>
                </TableRow>
              ))}
            </TableBody>
          </Table>
        </TableContainer>
      </Paper>

      <PDFPreviewModal open={pdfModalOpen} onClose={() => setPdfModalOpen(false)} pdfUrl={selectedPdf} />

      <Snackbar open={toastOpen} autoHideDuration={4000} onClose={() => setToastOpen(false)}>
        <Alert severity="success" sx={{ width: '100%', bgcolor: '#10B981', color: '#000', fontWeight: 700 }}>
          Legal Notice PDF successfully dispatched to Provider Nodal Email!
        </Alert>
      </Snackbar>
    </Box>
  );
}

==> ./src/theme/theme.js <==
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
    background: {
      default: '#0A0E1A',
      paper: '#111827',
    },
    primary: {
      main: '#2563EB', // Vibrant Blue
      light: '#60A5FA',
      dark: '#1D4ED8',
    },
    secondary: {
      main: '#10B981', // Emerald Green
    },
    warning: {
      main: '#F59E0B',
    },
    error: {
      main: '#EF4444',
    },
    text: {
      primary: '#F9FAFB',
      secondary: '#9CA3AF',
    },
  },
  typography: {
    fontFamily: '"Inter", "Roboto", "Helvetica", "Arial", sans-serif',
    h5: {
      fontWeight: 700,
    },
    h6: {
      fontWeight: 600,
    },
  },
  components: {
    MuiButton: {
      styleOverrides: {
        root: {
          borderRadius: 8,
          textTransform: 'none',
          fontWeight: 600,
        },
      },
    },
    MuiPaper: {
      styleOverrides: {
        root: {
          backgroundImage: 'none',
        },
      },
    },
  },
});

export default theme;

==> ./vite.config.js <==
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    host: true
  }
});
